'use client';
import { useState } from 'react';import { saveState, loadState } from '@/lib/storage';import { useRouter } from 'next/navigation';
export default function Setup(){const r=useRouter();const s=loadState();const [f,setF]=useState({studentName:s.profile?.studentName||'',subject:s.profile?.subject||'Math Grade 10',topic:s.profile?.topic||'Functions',difficulty:s.profile?.difficulty||'beginner',numberOfQuestions:s.profile?.numberOfQuestions||8});
return <div className='space-y-4'><h1 className='text-2xl font-bold'>Thiết lập bài chẩn đoán</h1><div className='card grid gap-3 md:grid-cols-2'>{['studentName','subject','topic','difficulty','numberOfQuestions'].map(k=><input key={k} className='bg-slate-800 p-2 rounded' value={(f as any)[k]} onChange={e=>setF({...f,[k]:k==='numberOfQuestions'?Number(e.target.value):e.target.value})} placeholder={k}/>)}<button className='bg-cyan-600 px-3 py-2 rounded' onClick={()=>{saveState({...s,profile:f});r.push('/test')}}>Lưu và bắt đầu</button></div></div>}
